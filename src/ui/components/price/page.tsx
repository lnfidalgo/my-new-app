/*import Price1 from "./priceTable";

export default function Price() {
  const slides = [<Price1 key={1} />];
  return (
    <div className="max-w-lg text-white">
      <Carousel>
        {slides.map((slide, index) => (
          <div key={index}>{slide}</div>
        ))}
      </Carousel>
    </div>
  );
}
*/
